---
phase: cms-admin-ui-for-cuti-archiving
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-display/src/api.ts
  - frontend-display/src/components/CmsDashboard.tsx
  - frontend-display/src/components/CmsDashboardResponsive.test.tsx
  - frontend-display/src/components/CmsDashboardSidebar.test.tsx
autonomous: true
requirements:
  - LVARCH-06
  - LVARCH-07
  - LVARCH-08
  - LVARCH-09
must_haves:
  truths:
    - "D-01: Implement a sub-tabs layout with two horizontal button tabs ('Cuti Aktif' and 'Cuti Terarsip') in the panel header of the 'Daftar Cuti Terdaftar' card."
    - "D-02: Change the 'Hapus' button/action in the active leave list to 'Arsipkan' with an archive icon (Archive from lucide-react) and trigger a confirmation dialog."
    - "D-03: Add a 'Pulihkan' (Restore) button with a restore/reload icon (RotateCcw or RefreshCw from lucide-react) for items in the 'Cuti Terarsip' list."
    - "D-04: Use the updated API client helper deleteLeaveItem (or alias it with deleteEmployeeLeave) and a new helper restoreLeaveItem to interface with the backend."
    - "D-05: Maintain a single list state and use the filter parameter of fetchEmployeeLeaves to reload the data dynamically on tab change."
  artifacts:
    - "frontend-display/src/api.ts: Updated fetchEmployeeLeaves helper, new restoreEmployeeLeave helper, and compatibility aliases deleteLeaveItem/restoreLeaveItem."
    - "frontend-display/src/components/CmsDashboard.tsx: Updated CMS Admin UI with sub-tabs layout, custom confirmation modals, and integration of archive/restore actions."
    - "frontend-display/src/components/CmsDashboardResponsive.test.tsx: Updated Jest mock for api.ts."
    - "frontend-display/src/components/CmsDashboardSidebar.test.tsx: Updated Jest mock for api.ts."
---

## <objective>
This plan updates the "Kelola Cuti" page in the CMS Admin panel to replace the "Hapus" (hard delete) action with an "Arsipkan" (soft-delete/archive) action. It provides a sub-tabs filter to toggle between "Cuti Aktif" and "Cuti Terarsip" lists, along with a "Pulihkan" (Restore) action for archived items, using a custom confirmation dialog.
## </objective>

## <execution_context>
- Ensure commands are run in the appropriate directory (`frontend-display`).
- Use Bun on the host (`C:\Users\yudhiar\.bun\bin\bun`) to build and run tests.
- Verify typescript check passes without errors.
## </execution_context>

## <context>
- [frontend-display/src/api.ts](file:///c:/Users/yudhiar/Downloads/oprek/Dev/tv/frontend-display/src/api.ts) — API client helpers.
- [frontend-display/src/components/CmsDashboard.tsx](file:///c:/Users/yudhiar/Downloads/oprek/Dev/tv/frontend-display/src/components/CmsDashboard.tsx) — Main CMS Admin UI component.
- [frontend-display/src/components/CmsDashboardResponsive.test.tsx](file:///c:/Users/yudhiar/Downloads/oprek/Dev/tv/frontend-display/src/components/CmsDashboardResponsive.test.tsx) — Responsive UI test file.
- [frontend-display/src/components/CmsDashboardSidebar.test.tsx](file:///c:/Users/yudhiar/Downloads/oprek/Dev/tv/frontend-display/src/components/CmsDashboardSidebar.test.tsx) — Sidebar UI test file.
## </context>

## <tasks>
<tasks>
  <task id="1" status="blocking">
    <name>Update frontend API client helper functions</name>
    <files>
      <file>frontend-display/src/api.ts</file>
    </files>
    <read_first>
      <file>frontend-display/src/api.ts</file>
    </read_first>
    <action>
      Modify `frontend-display/src/api.ts`:
      - Update `fetchEmployeeLeaves(filter?: 'active' | 'archived' | 'all')` to append `?filter=${filter}` to the URL if provided.
      - Add `restoreEmployeeLeave(id: string)` helper function that performs `POST /api/agenda/cuti/${id}/restore`.
      - Export `deleteLeaveItem` and `restoreLeaveItem` aliases.
    </action>
    <verify>
      Build the frontend using `bun run build` to ensure no syntax/type errors in api.ts.
    </verify>
    <acceptance_criteria>
      - `fetchEmployeeLeaves` supports the `filter` parameter.
      - `restoreEmployeeLeave` is defined and correctly calls the restore endpoint.
      - `deleteLeaveItem` and `restoreLeaveItem` aliases are exported.
    </acceptance_criteria>
    <done>false</done>
  </task>

  <task id="2" status="blocking">
    <name>Update CMS Dashboard components with sub-tabs and custom confirmation modals</name>
    <files>
      <file>frontend-display/src/components/CmsDashboard.tsx</file>
    </files>
    <read_first>
      <file>frontend-display/src/components/CmsDashboard.tsx</file>
    </read_first>
    <action>
      Modify `frontend-display/src/components/CmsDashboard.tsx`:
      - Import `Archive` and `RotateCcw` from `lucide-react`.
      - Add `cutiFilter` state initialized to `'active'`.
      - Add `cutiToArchive` and `cutiToRestore` states to control custom modal overlays.
      - Update `loadCutiData` to pass `cutiFilter` to `fetchEmployeeLeaves`.
      - Update `useEffect` to reload cuti list when `cutiFilter` changes.
      - Update `handleAddCuti` to reset `cutiFilter` to `'active'` and fetch.
      - Implement custom Tailwind overlay confirmation modals for Archiving and Restoring cuti items.
      - Restructure the "Daftar Cuti Terdaftar" panel header to show horizontal tabs.
      - Update desktop/mobile list views to conditionalize actions: show "Arsipkan" for active lists, and "Pulihkan" for archived lists.
    </action>
    <verify>
      Compile and build the project using `bun run build`.
    </verify>
    <acceptance_criteria>
      - Horizontal sub-tabs "Cuti Aktif" and "Cuti Terarsip" are rendered on the CmsDashboard.
      - Clicking "Arsipkan" triggers a custom Tailwind modal dialog.
      - Clicking "Pulihkan" on an archived item triggers a custom Tailwind modal dialog.
      - Restored items reappear in "Cuti Aktif".
      - Archived items are hidden from the "Cuti Aktif" tab.
    </acceptance_criteria>
    <done>false</done>
  </task>

  <task id="3">
    <name>Update frontend test mocks</name>
    <files>
      <file>frontend-display/src/components/CmsDashboardResponsive.test.tsx</file>
      <file>frontend-display/src/components/CmsDashboardSidebar.test.tsx</file>
    </files>
    <read_first>
      <file>frontend-display/src/components/CmsDashboardResponsive.test.tsx</file>
      <file>frontend-display/src/components/CmsDashboardSidebar.test.tsx</file>
    </read_first>
    <action>
      Update the `jest.mock('../api')` mock implementation in `CmsDashboardResponsive.test.tsx` and `CmsDashboardSidebar.test.tsx` to define `deleteEmployeeLeave`, `restoreEmployeeLeave`, `createEmployeeLeave`, `deleteLeaveItem`, and `restoreLeaveItem` mocks.
    </action>
    <verify>
      Run `bun test -- --watchAll=false` and verify all tests pass successfully.
    </verify>
    <acceptance_criteria>
      - Jest mocks include the new API helpers.
      - All tests pass without mock failures.
    </acceptance_criteria>
    <done>false</done>
  </task>
</tasks>

## <verification>
Execute the following verification steps:
1. Run compilation check on the frontend application:
   ```bash
   cd frontend-display
   C:\Users\yudhiar\\.bun\bin\bun run build
   ```
2. Run Jest test runner for the frontend:
   ```bash
   cd frontend-display
   C:\Users\yudhiar\\.bun\bin\bun test -- --watchAll=false
   ```
3. Run docker compose up and verify visual changes on the UI:
   - Check tabs switching and custom modal overlays.
## </verification>

## <success_criteria>
- Custom horizontal sub-tabs correctly filter the leave items on the frontend.
- Hard delete is completely removed and replaced with custom archive modal and soft-delete behavior.
- Restore button correctly triggers restore modal and moves archived items back to active list.
- All frontend tests compile and pass successfully.
## </success_criteria>

## Artifacts this phase produces
- **UI Tab Filter**: Horizontal sub-tabs for active/archived leaves.
- **Custom Modals**: Tailwind modals for confirming archiving/restoring.
- **API Helpers**: `restoreEmployeeLeave`, compatibility aliases in `api.ts`.
